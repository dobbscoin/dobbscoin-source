# List of fixed seed nodes for main network
#
# (BOB) public nodes, 2026-09-26. These are the addresses behind the DNS
# seeds in chainparams.cpp (seed.dobbscoin.info, seed1..10.dobbscoin.info and
# the five 23skidoo.info names all resolve to exactly these six), each checked
# to accept a TCP connection on port 19985 and to appear as a /EndTimes:0.13.8/
# peer in getpeerinfo on guru and vps3 (70.34.196.61, not a peer of either,
# answered a version handshake as /EndTimes:0.13.8/ at height 1,912,733).
# Outside peers seen in getpeerinfo were left out: none accepted a connection
# on 19985, so a new node could not use them as a seed.
#
# Update with contrib/seeds/makeseeds.py when a seeder dump is available, then
# regenerate src/chainparamsseeds.h with share/seeds/generate-seeds.py.

65.20.114.6
67.219.101.148
70.34.196.61
70.34.209.232
78.17.212.21
159.198.79.74
